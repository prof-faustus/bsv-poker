node_modules/
dist/
*.tsbuildinfo
.DS_Store
coverage/
vm/.data/
# Go build artifacts (binaries), NOT the relay/ and indexer/ package subdirectories.
*.exe
apps/relay-go/relay-go
apps/indexer-go/indexer-go
*.log
.env.local
target/
# Native desktop build output (obj/exe). The vendored WebView2 SDK under native/{include,lib} IS
# committed so the host builds standalone; only the compiler output is ignored.
apps/client-desktop/build/
