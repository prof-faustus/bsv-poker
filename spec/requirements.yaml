# Requirements register — generated by tools/extract-requirements.ts (REQ-ENG-002).
# The cited spec section is canonical; excerpts are truncated. Do not edit by hand.
# counts: core=115 app=108 total=223
requirements:
  - id: REQ-ARCH-001
    spec: core
    excerpt: "The engine is a pure function of inputs: `(orderedValidTxSet, ruleset) -> tableState`."
    module: "engine"
    testFamily: "property + integration"
  - id: REQ-ARCH-002
    spec: core
    excerpt: "The engine performs no I/O, no networking, no time reads, and no randomness except via injected, recorded sources."
    module: "engine"
    testFamily: "property + integration"
  - id: REQ-ARCH-003
    spec: core
    excerpt: "\"Now\" enters the engine only as an explicit parameter for timeout-eligibility, derived from chain/relay-anchored height/time."
    module: "engine"
    testFamily: "property + integration"
  - id: REQ-BUILD-001
    spec: core
    excerpt: "Monorepo layout (see §16): /spec, /packages, /apps, /vm, /tests."
    module: "build / ci"
    testFamily: "CI"
  - id: REQ-BUILD-002
    spec: core
    excerpt: "Reproducible builds: pinned toolchains, locked lockfiles, recorded artifact hashes."
    module: "build / ci"
    testFamily: "CI"
  - id: REQ-BUILD-003
    spec: core
    excerpt: "CI stages: typecheck, lint (Power-of-Ten), unit+property, interpreter (Genesis), integration, build image, e2e-in-image, reproduce, traceability."
    module: "build / ci"
    testFamily: "CI"
  - id: REQ-BUILD-004
    spec: core
    excerpt: "Releases: Windows installer (Tauri, signed), web bundle, VM/container image, from the same commit with recorded hashes."
    module: "build / ci"
    testFamily: "CI"
  - id: REQ-CRYPTO-001
    spec: core
    excerpt: "Withholding a reveal after committing triggers a deterministic penalty/fallback (timeout forfeiture or committed fallback-seed)."
    module: "crypto-mentalpoker"
    testFamily: "property + interpreter + conformance"
  - id: REQ-CRYPTO-002
    spec: core
    excerpt: "No player may sample or alter `r_p` after observing any other `r_q`; binding by commit hash + ordering."
    module: "crypto-mentalpoker"
    testFamily: "property + interpreter + conformance"
  - id: REQ-CRYPTO-003
    spec: core
    excerpt: "Canonical party order = lexicographic order of long-term public keys (33-byte SEC-1); published in setup state."
    module: "crypto-mentalpoker"
    testFamily: "property + interpreter + conformance"
  - id: REQ-CRYPTO-004
    spec: core
    excerpt: "Each shuffle stage is committed `c_p = H(state ‖ scalars ‖ permutation)`; OP_RETURN acceptable for these dead-end commits."
    module: "crypto-mentalpoker"
    testFamily: "property + interpreter + conformance"
  - id: REQ-CRYPTO-005
    spec: core
    excerpt: "Mid-protocol substitution of a card's ciphertext/commitment is rejected by byte-equality across transitions and AEAD integrity."
    module: "crypto-mentalpoker"
    testFamily: "property + interpreter + conformance"
  - id: REQ-CRYPTO-006
    spec: core
    excerpt: "Fair-play transactions generated after shuffle/encryption, before reveal; locking script from in-script EC routines."
    module: "crypto-mentalpoker"
    testFamily: "property + interpreter + conformance"
  - id: REQ-CRYPTO-007
    spec: core
    excerpt: "Fix one square-root branch for shuffle keys; keep it consistent; treat both-branch exposure as a factor-2 reduction."
    module: "crypto-mentalpoker"
    testFamily: "property + interpreter + conformance"
  - id: REQ-CRYPTO-008
    spec: core
    excerpt: "The build MUST NOT present Mode A while claiming Mode B's property; active mode recorded in ruleset."
    module: "crypto-mentalpoker"
    testFamily: "property + interpreter + conformance"
  - id: REQ-CRYPTO-009
    spec: core
    excerpt: "Fair-play scaling is a measured risk; measure script size before single-script use; fallback = per-card/per-batch fair-play."
    module: "crypto-mentalpoker"
    testFamily: "interpreter + measured (§19.C)"
  - id: REQ-CRYPTO-010
    spec: core
    excerpt: "Shuffle is N-of-N over the currently-seated set; each hand is a fresh N-party shuffle (new seed/keys/order)."
    module: "crypto-mentalpoker"
    testFamily: "property + integration"
  - id: REQ-CRYPTO-011
    spec: core
    excerpt: "Sit-out/join take effect between hands only; participant set frozen at hand start."
    module: "crypto-mentalpoker"
    testFamily: "property + integration"
  - id: REQ-CRYPTO-012
    spec: core
    excerpt: "No partial reshuffle of an in-progress deck; mid-hand disconnect handled by timeout/recovery."
    module: "crypto-mentalpoker"
    testFamily: "property + adversarial"
  - id: REQ-DATA-001
    spec: core
    excerpt: "Web: IndexedDB; desktop+relay: SQLite."
    module: "persistence / engine"
    testFamily: "unit + replay (§14.2)"
  - id: REQ-DATA-002
    spec: core
    excerpt: "A transcript is the ordered valid tx set plus the commit/reveal material to re-derive state; exportable."
    module: "persistence / engine"
    testFamily: "unit + replay (§14.2)"
  - id: REQ-DATA-003
    spec: core
    excerpt: "Replay reconstructs the hand from transcript + ruleset hash, byte-identical, offline; divergence is a defect."
    module: "persistence / engine"
    testFamily: "replay (§14.2)"
  - id: REQ-DATA-004
    spec: core
    excerpt: "Via VA: selective-disclosure bundle proving a figure is genuine+anchored, revealing nothing else; boundary surfaced."
    module: "persistence / engine"
    testFamily: "integration"
  - id: REQ-DEP-001
    spec: core
    excerpt: "Core depends only on contracts CT/BS/VA/OB via an adapter layer; each has a fake for orchestration tests."
    module: "adapters"
    testFamily: "conformance + integration"
  - id: REQ-DEP-002
    spec: core
    excerpt: "A repo API change is absorbed in its adapter; no change propagates into engine/FSMs/UI."
    module: "adapters"
    testFamily: "conformance + integration"
  - id: REQ-DEP-003
    spec: core
    excerpt: "A single conformance suite per contract is run against both the fake and the real adapter; both must pass."
    module: "adapters"
    testFamily: "conformance"
  - id: REQ-DEP-004
    spec: core
    excerpt: "Security-critical behaviours (shuffle, reveal, fair-play, signing) tested against real implementations, never fakes."
    module: "adapters"
    testFamily: "conformance + interpreter"
  - id: REQ-ENG-001
    spec: core
    excerpt: "Built to NPR 7150.2 assurance + a documented Power-of-Ten adaptation (not literal compliance; rule 3/pointers N/A)."
    module: "(cross-cutting)"
    testFamily: "CI gates (§16)"
  - id: REQ-ENG-002
    spec: core
    excerpt: "Every REQ-* is an entry in a machine-readable register with id/text/section/module/tests/status; validated in CI."
    module: "(cross-cutting)"
    testFamily: "CI gates (§16)"
  - id: REQ-ENG-003
    spec: core
    excerpt: "A traceability matrix maps every requirement → code → test; CI fails on any untested requirement or untraced consensus file."
    module: "(cross-cutting)"
    testFamily: "CI gates (§16)"
  - id: REQ-ENG-004
    spec: core
    excerpt: "Every stated boundary/limit (P7/P8) is asserted in the source so no change silently papers over it."
    module: "(cross-cutting)"
    testFamily: "CI gates (§16)"
  - id: REQ-FSM-001
    spec: core
    excerpt: "A game module implements GameModule (init/getLegalActions/apply/isTimeoutEligible/isHandComplete/settle); no I/O."
    module: "engine / game-*"
    testFamily: "property + e2e"
  - id: REQ-FSM-002
    spec: core
    excerpt: "The full Hold'em transition table is specified in §19.E; the prose flow is normative for phase order + two-exit rule."
    module: "engine / game-holdem"
    testFamily: "property + e2e"
  - id: REQ-FSM-003
    spec: core
    excerpt: "Down-card = drawn then privately revealed to holder; up-card = drawn then N-of-N publicly revealed (with timeout-default)."
    module: "engine / game-stud / game-razz"
    testFamily: "property + e2e"
  - id: REQ-FSM-004
    spec: core
    excerpt: "Draw = surrender chosen concealed cards to dead-hand without reveal + draw equal fresh concealed cards from undealt deck."
    module: "engine / game-draw"
    testFamily: "property + e2e"
  - id: REQ-FSM-005
    spec: core
    excerpt: "Stud/razz betting order is determined by the exposed board, not the button (a distinct ordering function)."
    module: "engine / game-stud / game-razz"
    testFamily: "property + e2e"
  - id: REQ-FSM-006
    spec: core
    excerpt: "Omaha = Hold'em FSM with two overrides: deal 4 hole cards; showdown uses Omaha exactly-2+3 evaluator."
    module: "game-omaha"
    testFamily: "property + e2e + vectors"
  - id: REQ-FSM-007
    spec: core
    excerpt: "Omaha Hi-Lo (Omaha-8) adds an eight-or-better low split; distinct test-vectored path; off unless ruleset.hiLo."
    module: "game-omaha"
    testFamily: "vectors + e2e"
  - id: REQ-FSM-008
    spec: core
    excerpt: "8-handed stud/razz deck-exhaustion: final card dealt as a single shared community up-card; else 7-handed max."
    module: "game-stud / game-razz"
    testFamily: "property + e2e"
  - id: REQ-FSM-009
    spec: core
    excerpt: "Draw preserves concealment: discards to dead-hand without reveal; replacements private; count public, identities not."
    module: "game-draw"
    testFamily: "property + e2e"
  - id: REQ-FSM-010
    spec: core
    excerpt: "Five-card-draw S4 DRAW timeout-default is stand-pat (draw zero)."
    module: "game-draw"
    testFamily: "property + adversarial"
  - id: REQ-FSM-011
    spec: core
    excerpt: "Razz = stud FSM with three overrides: bring-in = highest up-card; post-3rd order = best low first; ace-to-five low showdown."
    module: "game-razz"
    testFamily: "property + e2e + vectors"
  - id: REQ-NET-001
    spec: core
    excerpt: "The relay is transport + indexing only; never the source of truth."
    module: "relay-go / indexer-go"
    testFamily: "integration + adversarial"
  - id: REQ-NET-002
    spec: core
    excerpt: "Tier A discovery = hosted relay + LAN auto-discovery (Phase 1); version/verack peer layer at Phase 5."
    module: "relay-go"
    testFamily: "integration"
  - id: REQ-NET-003
    spec: core
    excerpt: "Every action sent simultaneously to the network (canonical) and to table peers (speed); speed never overrides canonical."
    module: "relay-go / indexer-go"
    testFamily: "integration + adversarial"
  - id: REQ-NET-004
    spec: core
    excerpt: "Via BS.node: broadcast; outpoint/UTXO status; double-spend-attempt status; headers/blocks; mempool conflicts."
    module: "indexer-go"
    testFamily: "integration"
  - id: REQ-NET-005
    spec: core
    excerpt: "One spendable phase-right per phase; conflicting actions cannot both be valid; accepted = referenced by next accepted transition."
    module: "engine / indexer-go"
    testFamily: "adversarial"
  - id: REQ-NET-006
    spec: core
    excerpt: "Deterministic local tie-break: first valid tx referenced by a threshold of peers; final tie-break by lexicographic txid."
    module: "indexer-go"
    testFamily: "adversarial"
  - id: REQ-NET-007
    spec: core
    excerpt: "A client can request the transcript gap and rebuild current state deterministically from the valid tx set."
    module: "relay-go / engine"
    testFamily: "integration"
  - id: REQ-POKER-001
    spec: core
    excerpt: "The deck encoding is fixed and identical to the one bound into the shuffle (§4) and tx schemas (§6)."
    module: "protocol-types / hand-eval"
    testFamily: "unit + vectors (§19.D)"
  - id: REQ-POKER-002
    spec: core
    excerpt: "rulesetHash = H(canonicalSerialize(Ruleset)); computed once, displayed, bound into every transaction."
    module: "engine / tx-builder"
    testFamily: "unit + interpreter"
  - id: REQ-POKER-003
    spec: core
    excerpt: "The evaluator is pure/deterministic and backed by §19.D vectors (categories, wheel, kickers, Omaha-2+3, low)."
    module: "hand-eval"
    testFamily: "unit + vectors (§19.D)"
  - id: REQ-POKER-004
    spec: core
    excerpt: "Best-5-of-7 enumerates C(7,5)=21 subsets and takes the max by compare."
    module: "hand-eval"
    testFamily: "unit + vectors"
  - id: REQ-POKER-005
    spec: core
    excerpt: "Omaha enumerates C(4,2)·C(5,3)=60 combinations and takes the max; mandatory distinct path."
    module: "hand-eval"
    testFamily: "unit + vectors (§19.D)"
  - id: REQ-POKER-006
    spec: core
    excerpt: "Low evaluation returns a comparable where lower is better (ace-to-five); pairs penalised; distinct path."
    module: "hand-eval"
    testFamily: "unit + vectors (§19.D)"
  - id: REQ-POKER-007
    spec: core
    excerpt: "Evaluator correct first, fast second; bounded enumeration; any LUT optimisation must reproduce the reference bit-for-bit."
    module: "hand-eval"
    testFamily: "unit + vectors"
  - id: REQ-POKER-008
    spec: core
    excerpt: "Betting is a strategy behind one BettingStructure interface (NL/PL/FL); legalBets/applyBet."
    module: "engine / betting"
    testFamily: "unit + property"
  - id: REQ-POKER-009
    spec: core
    excerpt: "Betting machine tracks stacks, committed-this-round/-hand, bet-to-call, last full raise, all-in, who acted, round-close."
    module: "engine / betting"
    testFamily: "unit + property"
  - id: REQ-POKER-010
    spec: core
    excerpt: "Round closes when action returns to last aggressor with all live non-all-in matched; short all-in doesn't reopen unless a full raise."
    module: "engine / betting"
    testFamily: "unit + property"
  - id: REQ-POKER-011
    spec: core
    excerpt: "Pot engine computes main + ordered side pots from per-seat contributions on differing all-ins (§19.B)."
    module: "engine / pots"
    testFamily: "unit + vectors (§19.B)"
  - id: REQ-POKER-012
    spec: core
    excerpt: "Each pot awarded independently to the best eligible hand; ties split per §5.5.1."
    module: "engine / pots"
    testFamily: "unit + vectors"
  - id: REQ-POKER-013
    spec: core
    excerpt: "Odd chip deterministic (left-of-button default); suit tiebreak is house-rule, default OFF, never in hand-eval."
    module: "engine / pots"
    testFamily: "unit"
  - id: REQ-POKER-014
    spec: core
    excerpt: "Showdown: only contenders reveal, minimum required; last aggressor shows first; lose-only may muck."
    module: "engine"
    testFamily: "e2e"
  - id: REQ-POKER-015
    spec: core
    excerpt: "A revealed hand is verified against concealed-card commitments before it can win."
    module: "engine / script-templates-ts"
    testFamily: "interpreter"
  - id: REQ-POKER-016
    spec: core
    excerpt: "Settlement routes each pot to winner(s) and updates stacks; on-chain spend or BS channel update."
    module: "engine / tx-builder"
    testFamily: "integration"
  - id: REQ-POKER-017
    spec: core
    excerpt: "If settlement stalls, the recovery/timeout path resolves value deterministically; funds never stranded."
    module: "engine / tx-builder"
    testFamily: "adversarial"
  - id: REQ-PROD-001
    spec: core
    excerpt: "Wallet create/import; pluggable custody."
    module: "wallet-custody / ui-core"
    testFamily: "e2e"
  - id: REQ-PROD-002
    spec: core
    excerpt: "Lobby: list/create/join tables; presence of other players."
    module: "app/sdk / relay-go / ui-core"
    testFamily: "e2e"
  - id: REQ-PROD-003
    spec: core
    excerpt: "Ruleset + stake configuration with a displayed, hashed config."
    module: "ui-core / engine"
    testFamily: "e2e"
  - id: REQ-PROD-004
    spec: core
    excerpt: "Table view: seats, turn, board/up cards, pot(s), balances, timers, default-on-timeout text."
    module: "ui-core"
    testFamily: "component + e2e"
  - id: REQ-PROD-005
    spec: core
    excerpt: "Local private-hand inspection; never expose keys to the UI beyond the viewer path."
    module: "ui-core / wallet-custody"
    testFamily: "e2e"
  - id: REQ-PROD-006
    spec: core
    excerpt: "Signing prompts that state exactly what is being signed."
    module: "ui-core"
    testFamily: "component + e2e"
  - id: REQ-PROD-007
    spec: core
    excerpt: "Fold-without-reveal action."
    module: "engine / ui-core"
    testFamily: "e2e"
  - id: REQ-PROD-008
    spec: core
    excerpt: "Showdown reveal of only what is required."
    module: "engine / ui-core"
    testFamily: "e2e"
  - id: REQ-PROD-009
    spec: core
    excerpt: "Deterministic settlement display + final balances."
    module: "engine / ui-core"
    testFamily: "e2e"
  - id: REQ-PROD-010
    spec: core
    excerpt: "Transcript export + deterministic offline replay."
    module: "persistence / ui-core"
    testFamily: "replay"
  - id: REQ-PROD-011
    spec: core
    excerpt: "Reconnect/resume into a live table."
    module: "relay-go / engine / ui-core"
    testFamily: "integration"
  - id: REQ-PROD-012
    spec: core
    excerpt: "Clear research/regtest banner; mainnet behind explicit flag."
    module: "ui-core / vm"
    testFamily: "e2e"
  - id: REQ-TEST-001
    spec: core
    excerpt: "Unit (hand-eval, pots, betting, serialization vs §19.D/B vectors)."
    module: "tests/unit"
    testFamily: "meta (CI)"
  - id: REQ-TEST-002
    spec: core
    excerpt: "Replay equivalence + cross-client agreement (byte-identical)."
    module: "tests/property"
    testFamily: "meta (CI)"
  - id: REQ-TEST-003
    spec: core
    excerpt: "Every script template spend (pos+neg) executed through the real interpreter, Genesis on; neg fails inside interpreter."
    module: "tests/interpreter"
    testFamily: "meta (CI)"
  - id: REQ-TEST-004
    spec: core
    excerpt: "Scripted full heads-up Hold'em hand runs through the VM on regtest + the failure injections."
    module: "tests/e2e"
    testFamily: "meta (CI)"
  - id: REQ-TEST-005
    spec: core
    excerpt: "`reproduce` regenerates every committed vector and exits non-zero on mismatch; runs in the VM image."
    module: "tests/*"
    testFamily: "meta (CI)"
  - id: REQ-TEST-006
    spec: core
    excerpt: "Adversarial suite: disconnect/stale/timeout/withheld-reveal/conflict/eviction/fair-play-violation/substitution/etc."
    module: "tests/adversarial"
    testFamily: "meta (CI)"
  - id: REQ-TEST-007
    spec: core
    excerpt: "A phase is accepted only when its requirements trace to passing tests, reproduce is green, adversarial+e2e pass."
    module: "tests/*"
    testFamily: "meta (CI)"
  - id: REQ-TX-001
    spec: core
    excerpt: "CLTV/CSV are no-ops on post-Genesis BSV and MUST NOT enforce timing."
    module: "script-templates-ts"
    testFamily: "interpreter (§14.3)"
  - id: REQ-TX-002
    spec: core
    excerpt: "Timing enforced at the transaction level (nLockTime + nSequence under original replacement)."
    module: "tx-builder"
    testFamily: "interpreter"
  - id: REQ-TX-003
    spec: core
    excerpt: "Transaction-level timing + bond-forfeiture incentive is sufficient for the cooperative/timeout model."
    module: "tx-builder"
    testFamily: "interpreter + adversarial"
  - id: REQ-TX-004
    spec: core
    excerpt: "Opcode palette limited to post-Genesis-meaningful primitives (sig/hash/conditional/numeric-stack for EC routines)."
    module: "script-templates-ts"
    testFamily: "interpreter"
  - id: REQ-TX-005
    spec: core
    excerpt: "Every tx binds gid, rulesetHash, round, state hash, acting seat, economic state, commitments, successor commitment."
    module: "tx-builder"
    testFamily: "interpreter"
  - id: REQ-TX-006
    spec: core
    excerpt: "Two timeout layers: decision (default check-or-fold, never forced wager) and recovery (withheld reveal/stalled settlement)."
    module: "tx-builder"
    testFamily: "adversarial"
  - id: REQ-TX-007
    spec: core
    excerpt: "\"Now\" for timeouts from chain/relay-anchored height/time, not local wall-clock for consensus decisions."
    module: "engine / tx-builder"
    testFamily: "property"
  - id: REQ-TX-008
    spec: core
    excerpt: "A pre-signed fallback graph is signed before play for every stall (abort/quorum/deck/deal/action/reveal/settlement/close)."
    module: "tx-builder"
    testFamily: "interpreter + adversarial"
  - id: REQ-TX-009
    spec: core
    excerpt: "Dead-end commitments (shuffle stage, ruleset-hash) MAY use OP_RETURN."
    module: "script-templates-ts"
    testFamily: "interpreter"
  - id: REQ-TX-010
    spec: core
    excerpt: "Commitments that must stay in the spend-linked graph MUST be pushdata in a live script, not OP_RETURN."
    module: "script-templates-ts"
    testFamily: "interpreter"
  - id: REQ-TX-011
    spec: core
    excerpt: "Each template ships a positive test, negative battery (fail inside interpreter), and a measured wire-byte size vector."
    module: "script-templates-ts"
    testFamily: "interpreter (§14.3)"
  - id: REQ-TX-012
    spec: core
    excerpt: "Spending a Q_j UTXO requires a signature under Q_j per the active signing mode (A: reconstruct-at-reveal; B: threshold)."
    module: "tx-builder / wallet-custody"
    testFamily: "interpreter"
  - id: REQ-UI-001
    spec: core
    excerpt: "One TS/React UI core runs in the browser and inside Tauri; no business logic in the shell."
    module: "ui-core / client-*"
    testFamily: "component + e2e"
  - id: REQ-UI-002
    spec: core
    excerpt: "Web persists via IndexedDB; no localStorage/sessionStorage for load-bearing state; desktop SQLite."
    module: "client-web"
    testFamily: "component"
  - id: REQ-UI-003
    spec: core
    excerpt: "Interactions use explicit handlers, never `<form>` submit (avoids webview navigation side effects)."
    module: "ui-core"
    testFamily: "component"
  - id: REQ-UI-004
    spec: core
    excerpt: "The UI hides protocol complexity but never hides consequences (explicit default-on-timeout / committed-state text)."
    module: "ui-core"
    testFamily: "component + e2e"
  - id: REQ-UI-005
    spec: core
    excerpt: "Card decryption goes through the custody boundary; rendered face only in the controlled viewer path."
    module: "ui-core / wallet-custody"
    testFamily: "e2e"
  - id: REQ-UI-006
    spec: core
    excerpt: "Every signing prompt shows action, amounts, affected pot/state, and the exact intent/bytes signed; no silent signing."
    module: "ui-core"
    testFamily: "component + e2e"
  - id: REQ-VM-001
    spec: core
    excerpt: "A reproducible self-contained runtime launches node(regtest)+relay+client with no external services."
    module: "vm"
    testFamily: "e2e-in-image + reproduce"
  - id: REQ-VM-002
    spec: core
    excerpt: "Image contains the bonded-subsat-channel embedded node (regtest), the Go relay/indexer, the client, and a bootstrap."
    module: "vm"
    testFamily: "e2e-in-image"
  - id: REQ-VM-003
    spec: core
    excerpt: "Primary packaging: a reproducible container image + a one-liner that yields a playable stack."
    module: "vm"
    testFamily: "e2e-in-image"
  - id: REQ-VM-004
    spec: core
    excerpt: "Desktop: a Windows installer (Tauri) that embeds/supervises a local node+relay so a user double-clicks and plays."
    module: "client-desktop / vm"
    testFamily: "e2e"
  - id: REQ-VM-005
    spec: core
    excerpt: "A literal hypervisor VM image (OVA/qcow2), if required, is an extra artifact from the same composition (DECISION)."
    module: "vm"
    testFamily: "packaging"
  - id: REQ-VM-006
    spec: core
    excerpt: "The image build is reproducible (pinned toolchains, locked deps, recorded hashes)."
    module: "vm"
    testFamily: "CI"
  - id: REQ-VM-007
    spec: core
    excerpt: "Mainnet only behind an explicit named research flag; unmissable banner; regtest default everywhere."
    module: "vm / ui-core"
    testFamily: "e2e"
  - id: REQ-WALLET-001
    spec: core
    excerpt: "One long-term secp256k1 key per player; per-game/per-card scalars via HKDF bound to (gid,j); old-game keys reveal nothing."
    module: "wallet-custody"
    testFamily: "unit + conformance"
  - id: REQ-WALLET-002
    spec: core
    excerpt: "Distinct key sets for distinct jobs (signing vs card-encryption vs session); least authority."
    module: "wallet-custody"
    testFamily: "unit"
  - id: REQ-WALLET-003
    spec: core
    excerpt: "A Custody interface abstracts where keys live and signing happens (derive/sign/decryptToViewer/combineSignShare)."
    module: "wallet-custody"
    testFamily: "unit + conformance"
  - id: REQ-WALLET-004
    spec: core
    excerpt: "Whole-key handling follows the signing mode: Mode A reconstructs w_j at reveal (single-game keys, bounded window); Mode B threshold."
    module: "wallet-custody"
    testFamily: "unit + conformance"
  - id: REQ-WALLET-005
    spec: core
    excerpt: "In micro-betting mode, channel keys + the 1-sat bond managed via BS; presented as explicit signing actions."
    module: "wallet-custody"
    testFamily: "integration"
  - id: REQ-APP-001
    spec: app
    excerpt: "holds keys behind a custody boundary and derives table"
    module: "(app layer — see §A18)"
    testFamily: "(see §A16)"
  - id: REQ-APP-002
    spec: app
    excerpt: "sends every player action on **two paths"
    module: "(app layer — see §A18)"
    testFamily: "(see §A16)"
  - id: REQ-APP-003
    spec: app
    excerpt: "renders the table and obtains"
    module: "(app layer — see §A18)"
    testFamily: "(see §A16)"
  - id: REQ-APP-010
    spec: app
    excerpt: "The UI and app-services MUST treat the engine as a pure function (core §3.3, REQ-ARCH-001/002):"
    module: "(app layer — see §A18)"
    testFamily: "(see §A16)"
  - id: REQ-APP-011
    spec: app
    excerpt: "(see app spec)"
    module: "(app layer — see §A18)"
    testFamily: "(see §A16)"
  - id: REQ-APP-012
    spec: app
    excerpt: "no application package may import a dependency repo directly; it goes through"
    module: "(app layer — see §A18)"
    testFamily: "(see §A16)"
  - id: REQ-APP-020
    spec: app
    excerpt: "The supervisor MUST implement this lifecycle and expose it to the UI as a status stream:"
    module: "(app layer — see §A18)"
    testFamily: "(see §A16)"
  - id: REQ-APP-021
    spec: app
    excerpt: "ordered startup (node → indexer → relay); reverse-order shutdown. REQ-APP-022:"
    module: "(app layer — see §A18)"
    testFamily: "(see §A16)"
  - id: REQ-APP-022
    spec: app
    excerpt: "restart policy is bounded (max attempts, backoff) — both `TRACKED ASSUMPTION` values the build sets"
    module: "(app layer — see §A18)"
    testFamily: "(see §A16)"
  - id: REQ-APP-023
    spec: app
    excerpt: "`READY` is the only state in which the lobby permits creating/joining a table; other"
    module: "(app layer — see §A18)"
    testFamily: "(see §A16)"
  - id: REQ-APP-024
    spec: app
    excerpt: "Command/event families; exact signatures are Claude Code's, typed against core §15 and"
    module: "(app layer — see §A18)"
    testFamily: "(see §A16)"
  - id: REQ-APP-025
    spec: app
    excerpt: "(see app spec)"
    module: "(app layer — see §A18)"
    testFamily: "(see §A16)"
  - id: REQ-APP-026
    spec: app
    excerpt: "every IPC message is validated on both sides against its schema; an unrecognised or"
    module: "(app layer — see §A18)"
    testFamily: "(see §A16)"
  - id: REQ-APP-027
    spec: app
    excerpt: "ports are allocated and written to a runtime config the UI reads; no hard-coded ports."
    module: "(app layer — see §A18)"
    testFamily: "(see §A16)"
  - id: REQ-APP-028
    spec: app
    excerpt: "the SQLite store and the node's block/UTXO store live under a per-user data directory"
    module: "(app layer — see §A18)"
    testFamily: "(see §A16)"
  - id: REQ-APP-029
    spec: app
    excerpt: "the embedded node defaults to **regtest**;"
    module: "(app layer — see §A18)"
    testFamily: "(see §A16)"
  - id: REQ-APP-030
    spec: app
    excerpt: "refines core REQ-VM-007)."
    module: "(app layer — see §A18)"
    testFamily: "(see §A16)"
  - id: REQ-APP-040
    spec: app
    excerpt: "(see app spec)"
    module: "(app layer — see §A18)"
    testFamily: "(see §A16)"
  - id: REQ-APP-041
    spec: app
    excerpt: "(see app spec)"
    module: "(app layer — see §A18)"
    testFamily: "(see §A16)"
  - id: REQ-APP-042
    spec: app
    excerpt: "(see app spec)"
    module: "(app layer — see §A18)"
    testFamily: "(see §A16)"
  - id: REQ-APP-043
    spec: app
    excerpt: "(see app spec)"
    module: "(app layer — see §A18)"
    testFamily: "(see §A16)"
  - id: REQ-APP-044
    spec: app
    excerpt: "(see app spec)"
    module: "(app layer — see §A18)"
    testFamily: "(see §A16)"
  - id: REQ-APP-045
    spec: app
    excerpt: "(see app spec)"
    module: "(app layer — see §A18)"
    testFamily: "(see §A16)"
  - id: REQ-APP-050
    spec: app
    excerpt: "A single unidirectional store. Inputs: engine-derived `tableState` (§A2.2), connection status,"
    module: "(app layer — see §A18)"
    testFamily: "(see §A16)"
  - id: REQ-APP-051
    spec: app
    excerpt: "One view-model per screen (§A6), each a pure projection `(engineState, connStatus, custodyStatus,"
    module: "(app layer — see §A18)"
    testFamily: "(see §A16)"
  - id: REQ-APP-052
    spec: app
    excerpt: "Catalogued, each with props, states (loading/empty/error/ready), and a test obligation (Appendix"
    module: "(app layer — see §A18)"
    testFamily: "(see §A16)"
  - id: REQ-APP-053
    spec: app
    excerpt: "Explicit handlers only (`onClick`/`onChange`); **no** HTML `<form>` submit (core §11.3,"
    module: "(app layer — see §A18)"
    testFamily: "(see §A16)"
  - id: REQ-APP-054
    spec: app
    excerpt: "(see app spec)"
    module: "(app layer — see §A18)"
    testFamily: "(see §A16)"
  - id: REQ-APP-060
    spec: app
    excerpt: "(see app spec)"
    module: "(app layer — see §A18)"
    testFamily: "(see §A16)"
  - id: REQ-APP-070
    spec: app
    excerpt: "(see app spec)"
    module: "(app layer — see §A18)"
    testFamily: "(see §A16)"
  - id: REQ-APP-071
    spec: app
    excerpt: "(see app spec)"
    module: "(app layer — see §A18)"
    testFamily: "(see §A16)"
  - id: REQ-APP-072
    spec: app
    excerpt: "(see app spec)"
    module: "(app layer — see §A18)"
    testFamily: "(see §A16)"
  - id: REQ-APP-073
    spec: app
    excerpt: "(see app spec)"
    module: "(app layer — see §A18)"
    testFamily: "(see §A16)"
  - id: REQ-APP-074
    spec: app
    excerpt: "(see app spec)"
    module: "(app layer — see §A18)"
    testFamily: "(see §A16)"
  - id: REQ-APP-075
    spec: app
    excerpt: "(see app spec)"
    module: "(app layer — see §A18)"
    testFamily: "(see §A16)"
  - id: REQ-APP-076
    spec: app
    excerpt: "(see app spec)"
    module: "(app layer — see §A18)"
    testFamily: "(see §A16)"
  - id: REQ-APP-080
    spec: app
    excerpt: "(see app spec)"
    module: "(app layer — see §A18)"
    testFamily: "(see §A16)"
  - id: REQ-APP-081
    spec: app
    excerpt: "(see app spec)"
    module: "(app layer — see §A18)"
    testFamily: "(see §A16)"
  - id: REQ-APP-082
    spec: app
    excerpt: "(see app spec)"
    module: "(app layer — see §A18)"
    testFamily: "(see §A16)"
  - id: REQ-APP-083
    spec: app
    excerpt: "(see app spec)"
    module: "(app layer — see §A18)"
    testFamily: "(see §A16)"
  - id: REQ-APP-090
    spec: app
    excerpt: "- Local action round-trip (UI action → signed → on speed path to peers): target `TRACKED"
    module: "(app layer — see §A18)"
    testFamily: "(see §A16)"
  - id: REQ-APP-091
    spec: app
    excerpt: "- No table can be frozen by one absent/malicious participant (core P4); the timeout-default path"
    module: "(app layer — see §A18)"
    testFamily: "(see §A16)"
  - id: REQ-APP-092
    spec: app
    excerpt: "- Bounded working memory in the state-derivation hot path (Power-of-Ten adaptation, §A17): pre-sized"
    module: "(app layer — see §A18)"
    testFamily: "(see §A16)"
  - id: REQ-APP-093
    spec: app
    excerpt: "- Seats 2–9 (core D2); Phase 1 fixed at 2; the UI and state model MUST handle the full envelope"
    module: "(app layer — see §A18)"
    testFamily: "(see §A16)"
  - id: REQ-APP-094
    spec: app
    excerpt: "- Desktop: Windows (primary; the installer target). The shared core is OS-agnostic; macOS/Linux"
    module: "(app layer — see §A18)"
    testFamily: "(see §A16)"
  - id: REQ-APP-100
    spec: app
    excerpt: "(see app spec)"
    module: "(app layer — see §A18)"
    testFamily: "(see §A16)"
  - id: REQ-APP-101
    spec: app
    excerpt: "Keys live only inside the custody boundary (Rust side / isolated worker). At rest they are wrapped"
    module: "(app layer — see §A18)"
    testFamily: "(see §A16)"
  - id: REQ-APP-102
    spec: app
    excerpt: "(see app spec)"
    module: "(app layer — see §A18)"
    testFamily: "(see §A16)"
  - id: REQ-APP-103
    spec: app
    excerpt: "Every input crossing a trust boundary — IPC messages, relay/peer messages, persisted records,"
    module: "(app layer — see §A18)"
    testFamily: "(see §A16)"
  - id: REQ-APP-104
    spec: app
    excerpt: "(see app spec)"
    module: "(app layer — see §A18)"
    testFamily: "(see §A16)"
  - id: REQ-APP-105
    spec: app
    excerpt: "The installer is signed; the build is reproducible with pinned toolchains, locked lockfiles, and"
    module: "(app layer — see §A18)"
    testFamily: "(see §A16)"
  - id: REQ-APP-106
    spec: app
    excerpt: "The desktop binds local services to loopback by default; any non-loopback exposure is explicit and"
    module: "(app layer — see §A18)"
    testFamily: "(see §A16)"
  - id: REQ-APP-110
    spec: app
    excerpt: "(see app spec)"
    module: "(app layer — see §A18)"
    testFamily: "(see §A16)"
  - id: REQ-APP-111
    spec: app
    excerpt: "On any ambiguity affecting value or state, the client does **not** act: it does not emit a"
    module: "(app layer — see §A18)"
    testFamily: "(see §A16)"
  - id: REQ-APP-112
    spec: app
    excerpt: "The app distinguishes \"playable\", \"degraded\" (e.g. relay down but canonical path alive — play"
    module: "(app layer — see §A18)"
    testFamily: "(see §A16)"
  - id: REQ-APP-113
    spec: app
    excerpt: "(see app spec)"
    module: "(app layer — see §A18)"
    testFamily: "(see §A16)"
  - id: REQ-APP-114
    spec: app
    excerpt: "(see app spec)"
    module: "(app layer — see §A18)"
    testFamily: "(see §A16)"
  - id: REQ-APP-120
    spec: app
    excerpt: "Structured, levelled logs (debug/info/warn/error), per component, with correlation IDs (table/round/"
    module: "(app layer — see §A18)"
    testFamily: "(see §A16)"
  - id: REQ-APP-121
    spec: app
    excerpt: "Counters/gauges/histograms for: service health and restarts (desktop), connection state and"
    module: "(app layer — see §A18)"
    testFamily: "(see §A16)"
  - id: REQ-APP-122
    spec: app
    excerpt: "A trace per player action across app-services → SDK → send (both paths) → acceptance, for diagnosing"
    module: "(app layer — see §A18)"
    testFamily: "(see §A16)"
  - id: REQ-APP-123
    spec: app
    excerpt: "(see app spec)"
    module: "(app layer — see §A18)"
    testFamily: "(see §A16)"
  - id: REQ-APP-124
    spec: app
    excerpt: "Logs, metrics, traces, and diagnostic bundles MUST NOT contain key material, decrypted card faces, or"
    module: "(app layer — see §A18)"
    testFamily: "(see §A16)"
  - id: REQ-APP-130
    spec: app
    excerpt: "(see app spec)"
    module: "(app layer — see §A18)"
    testFamily: "(see §A16)"
  - id: REQ-APP-131
    spec: app
    excerpt: "Tables, players, transactions (the transcript), card lineage (minted→drawn→revealed|folded→"
    module: "(app layer — see §A18)"
    testFamily: "(see §A16)"
  - id: REQ-APP-132
    spec: app
    excerpt: "Persisted records are validated on read (§A10.4); a corrupt record is quarantined, not trusted."
    module: "(app layer — see §A18)"
    testFamily: "(see §A16)"
  - id: REQ-APP-133
    spec: app
    excerpt: "Transcript retention policy is a `TRACKED ASSUMPTION` (e.g. keep N hands / configurable); export"
    module: "(app layer — see §A18)"
    testFamily: "(see §A16)"
  - id: REQ-APP-140
    spec: app
    excerpt: "(see app spec)"
    module: "(app layer — see §A18)"
    testFamily: "(see §A16)"
  - id: REQ-APP-141
    spec: app
    excerpt: "(see app spec)"
    module: "(app layer — see §A18)"
    testFamily: "(see §A16)"
  - id: REQ-APP-142
    spec: app
    excerpt: "(see app spec)"
    module: "(app layer — see §A18)"
    testFamily: "(see §A16)"
  - id: REQ-APP-143
    spec: app
    excerpt: "Releases (Windows installer signed, web bundle, image) come from the **same commit** with recorded"
    module: "(app layer — see §A18)"
    testFamily: "(see §A16)"
  - id: REQ-APP-150
    spec: app
    excerpt: "(see app spec)"
    module: "(app layer — see §A18)"
    testFamily: "(see §A16)"
  - id: REQ-APP-151
    spec: app
    excerpt: "documentation is versioned with the code, reviewed in the same pass, and CI fails if"
    module: "(app layer — see §A18)"
    testFamily: "(see §A16)"
  - id: REQ-APP-160
    spec: app
    excerpt: "Unit → property-based → integration → **interpreter-level script** → end-to-end (E2E) → adversarial/"
    module: "(app layer — see §A18)"
    testFamily: "(see §A16)"
  - id: REQ-APP-161
    spec: app
    excerpt: "Pure units: view-models (§A5.2) as `(state…) → props` projections; reducers; the persistence"
    module: "(app layer — see §A18)"
    testFamily: "(see §A16)"
  - id: REQ-APP-162
    spec: app
    excerpt: "- Property: determinism — the same ordered valid-tx set + ruleset yields byte-identical derived state"
    module: "(app layer — see §A18)"
    testFamily: "(see §A16)"
  - id: REQ-APP-163
    spec: app
    excerpt: "(see app spec)"
    module: "(app layer — see §A18)"
    testFamily: "(see §A16)"
  - id: REQ-APP-164
    spec: app
    excerpt: "(see app spec)"
    module: "(app layer — see §A18)"
    testFamily: "(see §A16)"
  - id: REQ-APP-165
    spec: app
    excerpt: "(see app spec)"
    module: "(app layer — see §A18)"
    testFamily: "(see §A16)"
  - id: REQ-APP-166
    spec: app
    excerpt: "(see app spec)"
    module: "(app layer — see §A18)"
    testFamily: "(see §A16)"
  - id: REQ-APP-167
    spec: app
    excerpt: "(see app spec)"
    module: "(app layer — see §A18)"
    testFamily: "(see §A16)"
  - id: REQ-APP-168
    spec: app
    excerpt: "(see app spec)"
    module: "(app layer — see §A18)"
    testFamily: "(see §A16)"
  - id: REQ-APP-169
    spec: app
    excerpt: "(see app spec)"
    module: "(app layer — see §A18)"
    testFamily: "(see §A16)"
  - id: REQ-APP-170
    spec: app
    excerpt: "Adopted as software-assurance **practice**: requirements with traceability (§A18), design before"
    module: "(app layer — see §A18)"
    testFamily: "(see §A16)"
  - id: REQ-APP-171
    spec: app
    excerpt: "(see app spec)"
    module: "(app layer — see §A18)"
    testFamily: "(see §A16)"
  - id: REQ-APP-172
    spec: app
    excerpt: "Adopted as security practice: threat-informed design (§A10.5), secure defaults (regtest, loopback,"
    module: "(app layer — see §A18)"
    testFamily: "(see §A16)"
  - id: REQ-APP-173
    spec: app
    excerpt: "(see app spec)"
    module: "(app layer — see §A18)"
    testFamily: "(see §A16)"
  - id: REQ-APP-180
    spec: app
    excerpt: "Every `REQ-APP-*` in this document is an entry in a machine-readable register"
    module: "(app layer — see §A18)"
    testFamily: "(see §A16)"
  - id: REQ-APP-181
    spec: app
    excerpt: "Every requirement → owning module → test(s) → acceptance gate. CI fails if any requirement has no"
    module: "(app layer — see §A18)"
    testFamily: "(see §A16)"
  - id: REQ-APP-182
    spec: app
    excerpt: "A phase is accepted only when: every requirement for that phase traces to passing tests; `reproduce`"
    module: "(app layer — see §A18)"
    testFamily: "(see §A16)"
  - id: REQ-APP-183
    spec: app
    excerpt: "The application spec is complete when Claude Code can build Phase 0 and Phase 1 **without asking a"
    module: "(app layer — see §A18)"
    testFamily: "(see §A16)"
  - id: REQ-APP-210
    spec: app
    excerpt: "Each game is a module implementing the core `GameModule` contract (core §7.1:"
    module: "(app layer — see §A18)"
    testFamily: "(see §A16)"
  - id: REQ-APP-211
    spec: app
    excerpt: "(see app spec)"
    module: "(app layer — see §A18)"
    testFamily: "(see §A16)"
  - id: REQ-APP-212
    spec: app
    excerpt: "A **game registry** lists available games with, per game: display name, supported seat range (core"
    module: "(app layer — see §A18)"
    testFamily: "(see §A16)"
  - id: REQ-APP-213
    spec: app
    excerpt: "(see app spec)"
    module: "(app layer — see §A18)"
    testFamily: "(see §A16)"
  - id: REQ-APP-214
    spec: app
    excerpt: "The table view (§A6.5) is **one** screen parameterised by a variant profile from the registry:"
    module: "(app layer — see §A18)"
    testFamily: "(see §A16)"
  - id: REQ-APP-215
    spec: app
    excerpt: "(see app spec)"
    module: "(app layer — see §A18)"
    testFamily: "(see §A16)"
  - id: REQ-APP-216
    spec: app
    excerpt: "(see app spec)"
    module: "(app layer — see §A18)"
    testFamily: "(see §A16)"
  - id: REQ-APP-217
    spec: app
    excerpt: "(see app spec)"
    module: "(app layer — see §A18)"
    testFamily: "(see §A16)"
  - id: REQ-APP-218
    spec: app
    excerpt: "(see app spec)"
    module: "(app layer — see §A18)"
    testFamily: "(see §A16)"
  - id: REQ-APP-219
    spec: app
    excerpt: "(see app spec)"
    module: "(app layer — see §A18)"
    testFamily: "(see §A16)"
  - id: REQ-APP-220
    spec: app
    excerpt: "(see app spec)"
    module: "(app layer — see §A18)"
    testFamily: "(see §A16)"
  - id: REQ-APP-221
    spec: app
    excerpt: "(see app spec)"
    module: "(app layer — see §A18)"
    testFamily: "(see §A16)"
  - id: REQ-APP-230
    spec: app
    excerpt: "(see app spec)"
    module: "(app layer — see §A18)"
    testFamily: "(see §A16)"
  - id: REQ-APP-231
    spec: app
    excerpt: "(see app spec)"
    module: "(app layer — see §A18)"
    testFamily: "(see §A16)"
  - id: REQ-APP-232
    spec: app
    excerpt: "The Grok-doc `revocable-nft-tee` repo is **not** in the author's published list (core §2.4 note); the"
    module: "(app layer — see §A18)"
    testFamily: "(see §A16)"
  - id: REQ-APP-240
    spec: app
    excerpt: "(see app spec)"
    module: "(app layer — see §A18)"
    testFamily: "(see §A16)"
  - id: REQ-APP-241
    spec: app
    excerpt: "The UI surfaces channel open/transfer/close/contested as signing actions; it shows that sub-satoshi"
    module: "(app layer — see §A18)"
    testFamily: "(see §A16)"
  - id: REQ-APP-242
    spec: app
    excerpt: "Stale-state broadcast, contested-close, and the `Q*` whole-satoshi reconciliation are tested against"
    module: "(app layer — see §A18)"
    testFamily: "(see §A16)"
